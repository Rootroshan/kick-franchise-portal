import { ComingSoon } from "@/components/admin/ComingSoon";
export default function Page() {
  return <ComingSoon title="Notifications" note="Platform alerts feed. The dashboard 'Needs attention' section surfaces the same signals today." />;
}
