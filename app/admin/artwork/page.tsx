import { ComingSoon } from "@/components/admin/ComingSoon";
export default function Page() {
  return <ComingSoon title="Artwork Hub" note="Kick-managed brand assets (upload, versioning, signed downloads). The assets module and API already exist; the admin screen is next." />;
}
